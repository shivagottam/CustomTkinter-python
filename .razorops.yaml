tasks:
  hello-job:
    runner: razorci/python
    steps:
   
    - checkout    
    - commands:
      - |
      - cd /opt
      - sonar-scanner -v
        sonar-scanner \
         -Dsonar.projectKey=pythonone \
         -Dsonar.sources=. \
         -Dsonar.host.url=http://${SONAR_WEB_HOST} \
         -Dsonar.login=${TOKEN} \
   
